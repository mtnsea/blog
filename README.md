# VitePress Portfolio Resume

一个基于 VitePress、Vue 和 GSAP 的作品集与个人简历展示站。

## Scripts

```bash
npm install
npm run dev
npm run build
```

首页内容在 `docs/components/PortfolioHome.vue`，站点配置在 `docs/.vitepress/config.ts`。
